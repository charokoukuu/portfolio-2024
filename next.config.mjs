/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', "upload.wikimedia.org", "protopedia.net", "cdn.qiita.com"],
    },
};

export default nextConfig;
